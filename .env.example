# Mapbox GL access token for the UK realtime map (RealtimeCard → UkMap).
# Create one at https://account.mapbox.com/access-tokens/ and copy it here as
# `.env.local`. Without it the card falls back to the self-contained CSS globe.
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here
